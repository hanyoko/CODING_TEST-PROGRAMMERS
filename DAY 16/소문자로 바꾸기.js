//나의 풀이
const solution = myString => myString.toLowerCase();

//toLowerCase() : 소문자로 변환해주는 메소드

//다른 사람의 풀이
function solution(num_list, n) {
    return num_list.indexOf(n) >= 0 ? 1 : 0
}

//indexOf()


