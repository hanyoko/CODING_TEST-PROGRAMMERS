//나의 풀이
function solution(array) {
    const arr = Math.max(...array);
    return [arr, array.indexOf(arr)]
}

//스프레드 함수를 이용해서 배열의 최댓값을 구해주고,
//indexOf를 이용해서 array에서 가장 큰 수의 index 값을 구해주었다 !

//다른 사람의 풀이

//대부분 비슷한 방법으로 풀었다 !
//이번에도 가장 좋아요가 많은 풀이법과 같았다 .. 뿌듯 + 1 추가 ..